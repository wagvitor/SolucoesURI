#include <stdio.h>
  
int main() {
  double A, B, MEDIA;
 
    A, B >= 0 && A, B <= 10.0;
 
    scanf("%lf",&A);
    scanf("%lf",&B);
 
    if ((A <= 10.0) && (B <= 10.0))
    MEDIA = ((A * 3.5) + (B * 7.5)) / 11;
 
 
    printf("MEDIA = %.5lf\n",MEDIA);
  
    return 0;
}
